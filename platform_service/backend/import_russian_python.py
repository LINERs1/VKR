import sqlite3
import os

DB_PATH = r"c:\Users\liner\Desktop\Diplom\platform_service\backend\app.db"

COURSE_ID = "python-pro"
COURSE_TITLE = "Профессия: Python Разработчик"
COURSE_DESC = "Интенсивный курс по Python на русском языке. Практика, алгоритмы, ООП и разработка реальных проектов."

LESSONS = [
    {
        "title": "Урок 1. Введение в Python и установка",
        "duration": "45 мин",
        "content": """# Введение в Python

Python — это высокоуровневый язык программирования общего назначения, который отлично подходит как для новичков, так и для профессионалов.

## Почему именно Python?
- **Простой синтаксис**: Код на Python читается как обычный английский текст.
- **Востребованность**: Используется в веб-разработке (Django, FastAPI), анализе данных (Pandas), машинном обучении (PyTorch, TensorFlow) и автоматизации.
- **Огромное комьюнити**: Вы всегда найдете ответ на свой вопрос на StackOverflow.

## Установка окружения

Для начала работы вам потребуется установить интерпретатор Python:
1. Перейдите на официальный сайт [python.org](https://python.org).
2. Скачайте последнюю версию для вашей ОС (Windows/macOS/Linux).
3. **Важно для Windows**: При установке обязательно поставьте галочку `Add Python to PATH`.

### Ваша первая программа
Создайте файл `hello.py` и напишите в нем следующий код:

```python
print("Привет, мир!")
```

Чтобы запустить код, откройте терминал и введите:
```bash
python hello.py
```

🎉 Поздравляем, вы только что написали свою первую программу!"""
    },
    {
        "title": "Урок 2. Переменные и базовые типы данных",
        "duration": "60 мин",
        "content": """# Переменные и типы данных

В Python вам не нужно явно указывать тип переменной при её создании. Интерпретатор сам понимает, какие данные вы в неё положили. Это называется **динамической типизацией**.

## Основные типы данных

- `int` (Целые числа): `age = 25`
- `float` (Дробные числа): `pi = 3.14`
- `str` (Строки): `name = "Иван"`
- `bool` (Логический тип): `is_student = True`

### Примеры работы с переменными

```python
# Базовая арифметика
a = 10
b = 3

print(a + b)  # 13
print(a - b)  # 7
print(a * b)  # 30
print(a / b)  # 3.3333333333333335
print(a // b) # 3 (целочисленное деление)
print(a % b)  # 1 (остаток от деления)
print(a ** b) # 1000 (возведение в степень)
```

## Работа со строками (f-строки)
С версии Python 3.6 появился очень удобный формат работы со строками:

```python
name = "Алексей"
age = 20
print(f"Меня зовут {name}, мне {age} лет.")
# Вывод: Меня зовут Алексей, мне 20 лет.
```

## Задание для самостоятельной работы
Создайте переменные для хранения цены товара и скидки в процентах. Напишите скрипт, который вычисляет и выводит итоговую цену."""
    },
    {
        "title": "Урок 3. Управляющие конструкции (Условия и Циклы)",
        "duration": "90 мин",
        "content": """# Условия и Циклы

Для управления потоком выполнения программы в Python используются операторы `if-elif-else`, а также циклы `for` и `while`.

## Условный оператор if

```python
age = 18

if age >= 18:
    print("Доступ разрешен")
elif age >= 14:
    print("Доступ с разрешения родителей")
else:
    print("Доступ запрещен")
```

> **Важно**: В Python блоки кода выделяются отступами (обычно 4 пробела). Здесь нет фигурных скобок `{}`!

## Цикл for

Цикл `for` чаще всего используется для перебора коллекций или выполнения действия заданное количество раз с помощью функции `range()`.

```python
# Вывод чисел от 0 до 4
for i in range(5):
    print(i)

# Перебор символов в строке
for char in "Python":
    print(char.upper())
```

## Цикл while

Цикл `while` выполняется до тех пор, пока условие истинно.

```python
counter = 5
while counter > 0:
    print(f"Обратный отсчет: {counter}")
    counter -= 1
print("Пуск!")
```

## Управление циклами: break и continue
- `break` немедленно завершает цикл.
- `continue` пропускает текущую итерацию и переходит к следующей."""
    },
    {
        "title": "Урок 4. Базовые структуры данных (Списки и Словари)",
        "duration": "120 мин",
        "content": """# Структуры данных

Чтобы хранить множество значений в одной переменной, мы используем коллекции. Самые популярные из них: **списки** и **словари**.

## Списки (Lists)
Список — это упорядоченная изменяемая коллекция. Списки создаются с помощью квадратных скобок `[]`.

```python
fruits = ["яблоко", "банан", "апельсин"]

# Добавление элемента
fruits.append("киви")

# Обращение по индексу (индексация начинается с нуля!)
print(fruits[0]) # "яблоко"

# Срезы (Slices)
print(fruits[1:3]) # ["банан", "апельсин"]
```

## Словари (Dictionaries)
Словарь — это коллекция пар "Ключ-Значение". Поиск по ключу в словаре происходит очень быстро.

```python
student = {
    "name": "Иван",
    "age": 21,
    "faculty": "Информатика"
}

# Получение значения
print(student["name"]) # Иван

# Добавление нового ключа
student["gpa"] = 4.8

# Перебор словаря
for key, value in student.items():
    print(f"{key}: {value}")
```

## Кортежи (Tuples) и Множества (Sets)
- **Кортеж** `(1, 2, 3)` — как список, но его нельзя изменять после создания. Занимает меньше памяти.
- **Множество** `{1, 2, 3}` — коллекция уникальных элементов. Отлично подходит для удаления дубликатов из списка: `list(set(my_list))`."""
    },
    {
        "title": "Урок 5. Функции: пишем переиспользуемый код",
        "duration": "80 мин",
        "content": """# Функции в Python

Если вы ловите себя на том, что копируете один и тот же кусок кода в разные места программы — пришло время написать функцию!

## Определение функции

Функции определяются с помощью ключевого слова `def`.

```python
def greet(name):
    \"\"\"Эта функция приветствует пользователя\"\"\"
    return f"Привет, {name}!"

message = greet("Мария")
print(message)
```

## Аргументы по умолчанию
Вы можете задать значения аргументов, которые будут использоваться, если пользователь их не передал.

```python
def calculate_total(price, tax=0.2):
    return price + (price * tax)

print(calculate_total(1000))       # Налог 20% по умолчанию -> 1200.0
print(calculate_total(1000, 0.1))  # Налог 10% -> 1100.0
```

## *args и **kwargs
Если вы не знаете заранее, сколько аргументов будет передано в функцию:

```python
# Произвольное число позиционных аргументов
def sum_all(*args):
    return sum(args)

print(sum_all(1, 2, 3, 4, 5)) # 15

# Произвольное число именованных аргументов
def print_user_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_user_info(name="Анна", role="Админ", age=25)
```"""
    }
]

def seed_russian_course():
    conn = sqlite3.connect(DB_PATH)
    cursor = conn.cursor()
    
    # 1. Удаляем весь мусор (старые английские или глючные курсы)
    cursor.execute("DELETE FROM lessons WHERE course_id='python-30-days'")
    cursor.execute("DELETE FROM courses WHERE id='python-30-days'")
    
    cursor.execute("DELETE FROM lessons WHERE course_id=?", (COURSE_ID,))
    cursor.execute("DELETE FROM courses WHERE id=?", (COURSE_ID,))
    
    # 2. Создаем красивый русский курс
    cursor.execute('''
        INSERT INTO courses (id, title, description, instructor, duration, icon, tags, lessons_count, students, rating) 
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
    ''', (
        COURSE_ID, 
        COURSE_TITLE, 
        COURSE_DESC, 
        "EduAI Академия", 
        "10 Часов", 
        "python", 
        "python, разработка", 
        len(LESSONS), 
        1540, 
        4.9
    ))
    
    # 3. Вставляем лекции
    for idx, lesson in enumerate(LESSONS):
        cursor.execute('''
            INSERT INTO lessons (course_id, title, content, duration) 
            VALUES (?, ?, ?, ?)
        ''', (COURSE_ID, lesson['title'], lesson['content'], lesson['duration']))
        
    conn.commit()
    conn.close()
    print("Отличный русский курс 'Python Разработчик' успешно добавлен в базу!")

if __name__ == '__main__':
    seed_russian_course()
